import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Path of Peril',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
